import React from 'react'
import DropdownField from 'shared/FormFields/DropdownField'

const Startup = ({ options }) => {
  return <DropdownField name="Startup" options={options} />
}

export default Startup
